

WebFont.load({
	google: {
		families: ['Maven Pro', 'Raleway', 'Abel']
	}
});

function setup() {
	createCanvas(window.innerWidth,200).parent('header');
}

function draw() {
	// background(0,0,0);
	// clear();
}